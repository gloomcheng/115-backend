# course-curatorial-design — evolution provenance

Status: legacy baseline adopted into the shared evolution system.

Baseline `SKILL.md` SHA-256: `af1a4ea50a0a09c88ab63d40ab256ee60e59b0c6a0bfdafe6c5a39a4a819d674`.

The baseline predates trace-backed clause mapping; this file does not claim that every existing instruction was evolved experimentally. Future material changes must cite validated pattern IDs, pass the shared gate, and append their accepted impact here. Rejected proposals remain in the central Wiki and must not be rewritten as active purpose.

## Accepted impacts

- `centralized-diagram-style-template` via `p-2026-08-31-central-diagram-template`:
  lesson ImageGen work reads one canonical locked style template and appends only an
  atomic subject brief. This prevents per-diagram style duplication and cross-session
  drift while preserving the existing one-candidate approval gate.
